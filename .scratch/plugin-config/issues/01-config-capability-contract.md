# 01 — config Capability 契约

**What to build:** 文档化并至少在 llm-openai 实现 `config.get/set/schema/reload`。get 对 secret 打码；set 写盘并更新内存。

**Status:** resolved

## Answer

llm-openai 与 session 均实现 config.*；CONTEXT.md 词条 Config Capability；schema 以运行时方法为准。

## Comments

（待填）
