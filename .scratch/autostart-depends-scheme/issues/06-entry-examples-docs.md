# 06 - 入口 -scheme、示例与 README 迁移

Status: done

**What to build:**

- CLI/Server 增加 `-scheme`，覆�?agent `defaultScheme`（启动时 set 或注入）�?- README：去装配叙事；Autostart/dependsOn/Scheme；Plugin Readme 推荐；`-assembly` deprecated 说明�?- `examples/*`：保�?assembly JSON 标注 reference-only；增加示�?scheme 片段或说明指�?agent config�?- 内置插件各补一份自由格�?README（命�?配置自述）�?
**Done when:** README 快速开始不依赖 assembly 白名单；每个仓库内置插件目录�?README.md�?