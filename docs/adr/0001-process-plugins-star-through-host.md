# 进程外插件 + 星型经 Host

插件以独立进程分发（配置 + 可执行 + 静态文件），运行时 Discovery/Assembly；一切跨插件调用与拦截只经 Host 路由，插件之间不直连。

进程内 `.so` 热插拔在 Windows 上不可靠且违背轻量；编译期组装又要求用户拿到源码才能扩展。进程边界换来可独立分发与崩溃隔离；星型拓扑保证 Waterfall/审批/审计只有一个策略平面，不会被旁路。代价是每次跨插件调用多一跳本机 IPC——相对模型延迟可接受。
