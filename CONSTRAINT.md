# CONSTRAINT

1. **文档同步**：接口/行为变更时同步改 `docs/`，文档保持简洁，不写空话。
2. **改动最小**：只做本次目标内的修改，不做顺手重构、清理或扩 scope。
3. **先共识再动手**：每次改动前先与用户对齐方案，达成一致后再改代码/文档。
4. **逐点讨论**：方案阶段一次只议一个决策点，对齐后再进下一点；不要一次铺开全量设计。
5. **进度文档**：会话内里程碑写入 `docs/progress/`，供后续交接；**一个需求完成后，删除 `docs/progress/` 下对应文档**。
