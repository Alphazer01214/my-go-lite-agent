# 03–04 — Host 工具/settle 渲染 + CLI 消费

**Status:** resolved

## Answer

CallTool 开始 `message_text`（Running …），结束 `summary_text`（JSONToPairs 截断）。settle 发 `markdown_text`；CLI streamedLive 时跳过以免双打。thinking 用 dim。
