// Command liteagent-cli is the CLI Render Medium entry: REPL, one-shot
// turns, session ops, and plugin diagnostics over the shared kernel.
package main

import "github.com/tomori/my-go-lite-agent/internal/app"

func main() { app.CLI() }
