dist/
*.exe
.idea/
sessions/
.DS_Store
/agent

# Local secrets — never commit real keys (keep *.example.json)
plugins/**/config.json
**/config.local.json
.env
.env.*

# Agent working data (reports, memory) — not product
.workbuddy/
.claude/
.zcode/
.legacy/